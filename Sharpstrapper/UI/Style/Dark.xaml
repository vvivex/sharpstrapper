<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Editor-specific overrides retained -->
    <SolidColorBrush x:Key="NewTextEditorBackground" Color="#2D2D2D" />
    <SolidColorBrush x:Key="NewTextEditorForeground" Color="White" />
    <SolidColorBrush x:Key="NewTextEditorLink" Color="#3A9CEA" />

    <!-- Background: use a consistent dark blue for the whole app -->
    <Color x:Key="ApplicationBackgroundColor">#FF0B2038</Color> <!-- dark blue -->
    <SolidColorBrush x:Key="ApplicationBackgroundBrush" Color="{StaticResource ApplicationBackgroundColor}" />

    <!-- Control fill: use matching dark-blue tones for controls -->
    <Color x:Key="ControlFillColorDefault">#FF13283F</Color>
    <SolidColorBrush x:Key="ControlFillColorDefaultBrush" Color="{StaticResource ControlFillColorDefault}" />

    <!-- Solid background used by some controls in Wpf.Ui; override to keep consistent dark-blue background -->
    <Color x:Key="SolidBackgroundFillColorBase">#FF0B2038</Color>
    <Color x:Key="SolidBackgroundFillColorBaseAlt">#FF0F2737</Color>
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseBrush" Color="{StaticResource SolidBackgroundFillColorBase}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseAltBrush" Color="{StaticResource SolidBackgroundFillColorBaseAlt}" />

    <!-- Accent colors: used for primary buttons and accent surfaces -->
    <Color x:Key="SystemAccentColorPrimary">#7A3BFF</Color>
    <Color x:Key="SystemAccentColorSecondary">#BFA3FF</Color>
    <Color x:Key="SystemAccentColorTertiary">#EDEBFF</Color>

    <!-- Accent gradients removed to avoid duplicate resource keys and revert to original behavior -->

    <!-- Ensure accent brush keys used by Wpf.Ui are present -->
    <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="{StaticResource SystemAccentColorPrimary}" />
    <SolidColorBrush x:Key="AccentFillColorDisabledBrush" Color="#28FFFFFF" />

    <!-- Text-on-accent colors (ensure good contrast over accent backgrounds) -->
    <Color x:Key="TextOnAccentFillColorPrimary">#FFFFFFFF</Color>
    <Color x:Key="TextOnAccentFillColorSecondary">#B3FFFFFF</Color>
    <Color x:Key="TextOnAccentFillColorDisabled">#87FFFFFF</Color>
    <Color x:Key="TextOnAccentFillColorSelectedText">#000000</Color>

    <SolidColorBrush x:Key="TextOnAccentFillColorPrimaryBrush" Color="{StaticResource TextOnAccentFillColorPrimary}" />
    <SolidColorBrush x:Key="TextOnAccentFillColorSecondaryBrush" Color="{StaticResource TextOnAccentFillColorSecondary}" />
    <SolidColorBrush x:Key="TextOnAccentFillColorDisabledBrush" Color="{StaticResource TextOnAccentFillColorDisabled}" />

    <!-- If controls ask for a default control stroke / surface, keep them readable on top of gradient -->
    <Color x:Key="ControlStrokeColorDefault">#40FFFFFF</Color>
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="{StaticResource ControlStrokeColorDefault}" />

</ResourceDictionary>
