<Application x:Class="Sharpstrapper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sharpstrapper"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Sharpstrapper.UI.Converters"
             ShutdownMode="OnExplicitShutdown"
             DispatcherUnhandledException="GlobalExceptionHandler">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
                <ResourceDictionary x:Name="CustomTheme" Source="UI/Style/Dark.xaml" /> <!-- NOTE: WpfUiWindow::ApplyTheme relies on this order. If you plan to change the order, please update the index in the function. -->
                <ResourceDictionary x:Name="Default" Source="UI/Style/Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Use Poppins as the main UI font (system-installed or user-provided). If Poppins isn't installed,
                 the system will fall back to the next available font. Keep the resource key name so
                 existing references (Rubik) continue to work. -->
            <FontFamily x:Key="Rubik">Poppins</FontFamily>

            <Style TargetType="Hyperlink">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground">
                            <Setter.Value>
                                <SolidColorBrush Color="{DynamicResource SystemAccentColorTertiary}" />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="TextDecorations" Value="Underline" />
                    </Trigger>
                </Style.Triggers>
                <Setter Property="TextDecorations" Value="None" />
                <Setter Property="Foreground">
                    <Setter.Value>
                        <SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" />
                    </Setter.Value>
                </Setter>
            </Style>

            <converters:StringFormatConverter x:Key="StringFormatConverter" />
            <converters:RangeConverter x:Key="RangeConverter" />
            <converters:EnumNameConverter x:Key="EnumNameConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>

